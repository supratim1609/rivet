name: rivet
description: A high-performance, type-safe, full-stack web framework for Dart with built-in JWT, WebSockets, and Admin Panel.
version: 2.0.0-dev.1
homepage: https://github.com/supratim1609/rivet
repository: https://github.com/supratim1609/rivet

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  dart_jsonwebtoken: ^3.3.1
  http: ^1.2.0
  mime: ^2.0.0
  mongo_dart: ^0.10.5
  mysql1: ^0.20.0
  path: ^1.9.1
  postgres: ^3.5.9
  watcher: ^1.1.4
  args: ^2.4.0
  analyzer: ^8.0.0
  shelf: ^1.4.0
  serverpod: ^2.9.2
  relic: ^0.10.0
  build: ^4.0.1
  source_gen: ^4.1.1

dev_dependencies:
  build_runner: ^2.9.0
  lints: ^6.0.0
  test: ^1.26.3
